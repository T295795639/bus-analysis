server:
  port: 8080

spring:
  application:
    name: bus-analysis
  datasource:
    url: jdbc:mysql://localhost:3306/bus_analysis?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 13603870586A8
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.nettiexj.bus.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

logging:
  level:
    com.nettiexj.bus.mapper: debug
